---
name: pb-ship
description: Run the pre-merge pb-suite gate: review, verification, regression-test classification, and an explicit human ship decision, never automatic merge.
---

# pb-ship

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-ship.md` procedure.
