---
name: pb-implement
description: Implement a feature spec-first with a declared verification plan, risk gating, test-first vertical slices, and pb-suite review handoff.
---

# pb-implement

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-implement.md` procedure.

The canonical command's model allocation is mandatory: when delegation is
permitted, use the cheapest capable model for bounded bulk work. The strongest
selected model must perform sanitization, evaluation, security/compliance
judgment, and final synthesis itself; sub-agents may provide evidence or
proposed patches only. If delegation is not permitted, execute slices
sequentially in the current session.
