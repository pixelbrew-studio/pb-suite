---
name: pb-prune-tests
description: Audit a test suite for likely-obsolete tests, broken imports, skipped files, and stale references without deleting anything by default.
---

# pb-prune-tests

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-prune-tests.md` procedure.
