---
name: pb-roast
description: Brutal, no-holds-barred roast of anything (website, file, dir, diff, PR, pasted text) via a frontier model on OpenCode. Default Grok, overridable via PB_ROAST_MODEL or --model. Read-only outside voice — cross-model on purpose.
---

# pb-roast

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-roast.md` procedure.
