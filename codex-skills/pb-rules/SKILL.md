---
name: pb-rules
description: Safely inject or update the canonical pb-suite workflow rules in project instructions, using a diff and explicit confirmation for replacements.
---

# pb-rules

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-rules.md` procedure.
