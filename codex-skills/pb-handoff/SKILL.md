---
name: pb-handoff
description: Compact the current session into a handoff a fresh agent can act on, recording the decisions, rejected approaches, and dead ends that git does not preserve.
---

# pb-handoff

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-handoff.md` procedure.
