---
name: pb-decisions
description: Surface the judgment calls behind the current work — retrospectively, the choices made without confidence, or forward, the unresolved choices drilled one at a time.
---

# pb-decisions

```bash
source "$HOME/.codex/skills/pb-bootstrap.sh"
```

Read `$PB_SUITE/codex-skills/ADAPTER.md`, then read and execute the complete
canonical `$PB_SUITE/commands/pb-decisions.md` procedure.
